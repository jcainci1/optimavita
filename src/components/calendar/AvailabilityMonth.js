import ServicesJSON from "../../../../components/data/schedule/availability_organization.json";

const AvailabilityMonth = ({ visibleDates }) => {
  console.log(visibleDates);
  return <></>;
};

export default AvailabilityMonth;
